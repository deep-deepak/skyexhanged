import React from 'react';
import { Container } from 'react-bootstrap';

const AboutSection = () => {
    return (
        <Container className="py-5">
            <p className="text-muted text-center">About section — design coming soon.</p>
        </Container>
    );
};

export default AboutSection;

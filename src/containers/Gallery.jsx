import React from 'react';
import { Container } from 'react-bootstrap';

const Gallery = () => {
    return (
        <Container className="py-5">
            <p className="text-muted text-center">Gallery — design coming soon.</p>
        </Container>
    );
};

export default Gallery;
